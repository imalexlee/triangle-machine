// create FrameData struct similar to old proj
// maybe a few functions in here to help with
// maybe have a frame buffer class that initializes all values. then the frame datas
// would just call .init(), .flush(), .create_descriptor_set() line 1403 vk_engine.cpp
